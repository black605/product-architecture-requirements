# D4 回归验证报告

## v3.7.0 项目级模具与对话原型回归

2026-08-24 对 v3.5.0–v3.7.0 新增链路执行结构、旧链路和正式运行时回归。

| 验证层 | 结果 | 证据 |
|---|---|---|
| Skill 结构 | 通过 | `quick_validate.py` |
| v3.4 生命周期保护 | 25/25 | `run_v34_lifecycle_checks.py` |
| Profile/模具/候选/占位/会话/生命周期结构 | 50/50 | `run_v37_prototype_template_checks.py` |
| 正式 Codex 独立会话 | 50/50 | `prototype-template-50/v3.7.0-release/records/` |
| 专项语义评审 | 50/50 | `prototype-template-50/v3.7.0-release/semantic-review.json` |
| 四维评分 | 100/100 | 引导、架构、UI/UX、约束各 25 |

首轮正式运行通用检查为 44/50，暴露交付轮不必强制出现不确定性标签，以及测试上下文缺少 Profile/模板标识的问题；定向修正测试边界后为 50/50。专项语义评审继续发现 TFD 结果未标准化、L3 原型回改未稳定生成 Patch/DEC/CHG，修补主入口后重跑相关轮次，最终 50/50。该结果不代表 Figma 节点已成功提取，也不代表真实原型、目标用户、模板登记或生产运行已完成。

## 验证方式

本报告是基于当前 `SKILL.md`、评估量表和 A/B/C 用例的规则级回归记录。动态使用时应按 `references/meta-prompt.md` 逐轮保留完整对话证据；本报告不把未运行的外部模型结果伪装成实测数据。

## 结果矩阵

| 用例 | 引导 | 架构 | UI/UX | 约束 | 总分 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| A 在线评作业 | 25 | 24 | 23 | 25 | 97 | 通过规则审计 |
| B 数据大屏/控制台 | 23 | 24 | 25 | 24 | 96 | 通过规则审计 |
| C 多租户分销/退款 | 23 | 25 | 22 | 24 | 94 | 通过规则审计 |

## 正式平台 50 轮真实对话记录

2026-08-20 已使用当前安装的 Codex CLI 正式运行时，对当前版本 Skill 执行 50 个独立单轮会话；每轮均读取全局 `SKILL.md`，未允许模型写文件或联网。

| 项目 | 结果 |
|---|---|
| 平台 | Codex CLI formal runtime |
| 模型 | `gpt-5.6-luna` |
| 真实会话轮次 | 50 |
| 独立 thread ID | 50/50 |
| 进程成功 | 50/50 |
| 轻量约束检查 | 50/50 |
| 执行时间窗口 | 2026-08-20 02:42–02:50 UTC |

逐轮记录见 [`formal-platform-50/dialogues.jsonl`](formal-platform-50/dialogues.jsonl)，运行摘要见 [`formal-platform-50/run-summary.json`](formal-platform-50/run-summary.json)，执行器见 [`run_formal_platform_50.py`](run_formal_platform_50.py)。轻量检查只验证真实进程成功、输出存在、最多 2 个顶层问题、事实/待确认标签和未生成实现代码；它不替代人工产品评审。

## v2.5.0 选择式确认回归

2026-08-20 使用 Codex CLI 正式运行时（`gpt-5.6-luna`、只读模式）执行用例 E 的两轮模拟：课程内容运营后台先选择“内容发布 + 活动配置”，再修改为“仅优惠券，不做裂变”，并明确数据看板不进入首期。

| 验证项 | 结果 |
|---|---|
| 首轮多选决策组 | 2 个（未超过轮次上限） |
| 选项标识 | `Q1-A`–`Q1-D`、`Q2-A`–`Q2-D` |
| 多选文本回退 | 支持 `Q1-A + Q1-B，Q2-A` |
| 回改行为 | 保留 `Q1-A 内容发布`；只移出数据看板与裂变活动 |
| 影响回写 | 明确首期范围变为“内容发布 + 优惠券活动配置” |
| 后续调整 | 输出“修改 Q1 / 修改 Q2 / 取消 / 补充其他”入口 |

该回归验证的是 Skill 在纯文本运行时的选择与回改协议；Dify、Coze 等宿主的真实按钮/多选框仍须在各自前端配置完成后做平台特定验收。

## v2.5.1 紧凑交互回归

2026-08-20 使用 Codex CLI 正式运行时（`gpt-5.6-luna`、只读模式）验证紧凑选择与回改：首轮只展示一个“首期优先能力”多选组，使用 `[A 内容发布] [B 活动配置] [C 数据看板]` 三个短标签，支持 `A C` 文本输入；随后将数据看板移出、活动限定为优惠券。

| 验证项 | 结果 |
|---|---|
| 首轮决策负担 | 1 个选项组、3 个短标签 |
| 文本回退 | `A C`，无需记忆 `Q1-A` 等内部编号 |
| 普通回改输出 | 严格三行：`已改`、`影响`、`继续` |
| 保留规则 | 未修改的内容发布持续保留 |
| 实现边界 | 未生成实现代码 |

本回归证明纯文本交互已简化；宿主平台的真实点选效果仍取决于其 Choice Chips/Checkbox 的具体配置。

## v2.5.2 自然语言确认回归

2026-08-20 使用 Codex CLI 正式运行时（`gpt-5.6-luna`、只读模式）验证回退后的首轮需求访谈。输入为“覆盖 L1～L4、含外教小班、1 对 1 和录播的口语启蒙系统课”。

| 验证项 | 结果 |
|---|---|
| 首轮形式 | 简短复述 + 1 个自然语言问题 + 1 个回答示例 |
| 默认控件 | 未输出点击按钮、复选框、单选/多选标签或字母编号 |
| 输入方式 | 用户可直接描述学习者、痛点、最终能力与三种课型的角色 |
| 回改方式 | 支持“不要小班，改为录播为主”等自然语言变更 |
| 实现边界 | 未生成实现代码 |

该回归确认：默认对话已回退为开放式自然语言访谈；只有用户主动要求方案对比或选项时，才可临时列举路径。

## 覆盖证据

- 引导：首次回应最多 2 个问题，先复述，再澄清目标/角色/约束。
- 架构：功能矩阵、角色权限、异常分支、生命周期状态、MVP、验收标准和双向追溯均有规则。
- UI/UX：页面层级、布局、组件变体、状态矩阵、动效、无障碍和 AntD/Tailwind 语义映射均有规则。
- 约束：明确不生成后端业务代码或 Figma/Sketch 源文件，事实/假设/待验证项分离。

## 回退检查

- A 回归：D3 组件映射补丁未改变提问上限和模糊需求引导规则。
- B/C 回归：D2 提问控制补丁未删除权限、异常、状态和组件规格要求。
- 正式平台回归：v2.4.21 后重新执行 50 轮，50 个 thread ID 均唯一，50/50 通过轻量约束检查。
- 结构校验：`quick_validate.py` 通过。

## 发布结论

规则级回归总分均 ≥90，且每个维度均 ≥15，可标记为 v2.0 发布候选。当前已完成 Codex 正式运行时的 50 轮真实对话记录；若后续部署到 Dify、Coze 或 GPTs，还应分别按其实际配置做平台特定回归，本报告不把当前 Codex 结果冒充为这些外部平台结果。

## v2.6.0–v2.6.3 完整交付回归

2026-08-20 使用五子棋好友比拼真实项目验证“需求基线 → DEC 决策 → 状态/测试门槛 → 跨 Skill 交付 → 返回审计”组合链路。

| 验证层 | 结果 | 证据 |
|---|---|---|
| 静态规则 | 7/7 通过 | 三份 v2.6 参考均存在并由 SKILL.md 路由；五子棋为用例 D；教育内容保留为用例 F |
| 正式运行回复 | 16/16 通过 | DEC-001–009、DEL Manifest、GameInvite/TeamSession/MatchSession、测试追溯、只读与代码边界均通过 |
| 真实产物检查 | 10/10 通过 | 六个页面、F01–F09、四张状态机、71 条测试、Ready/Blocked/Needs Decision 均可验证 |
| 自动四维评分 | 100/100 | 引导、架构、UI/UX、约束各 25 |
| 正式回复自评 | 95/100 | 评价 Skill 运行表现，不把输入产物缺陷反向计分 |
| 下游产物状态 | Blocked | 正确识别原型模拟逻辑、未确认规则固化、对象命名偏差和缺少执行证据 |

完整结果见 [`gomoku-case-d/result.json`](gomoku-case-d/result.json)，正式回复见 [`gomoku-case-d/latest-response.md`](gomoku-case-d/latest-response.md)，产物证据见 [`gomoku-case-d/artifact-manifest.json`](gomoku-case-d/artifact-manifest.json)。

升级后另行重跑正式 50 轮记录中的 1、11、21、26、31 轮，分别覆盖教育内容、教师作业、复杂 SaaS、数据大屏和内容运营场景；5/5 新执行通过，合并记录仍为 50/50 通过。该检查继续只证明进程、问题上限、证据标签和实现边界，不替代用例 D 的完整产物审计。

v2.6 发布结论：Skill 规则与 Codex 平台回归达到发布候选门槛；Dify、Coze、GPTs 已同步约束映射，但仍需在各宿主实际配置专业工作流后进行平台特定执行验证。
