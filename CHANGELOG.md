# Changelog

## [Unreleased]

### v4.0.0：可执行原型生产 Harness

- 修改位置：新增根命令 `harness`、`scripts/harness.py`、`assets/prototype-harness/`、Harness/Handoff schema、平台映射和发布文档。
- 修改原因：v3.7 的规则与结构评分不能证明最终页面无溢出、任务可达或素材合规，需要把约束做成可执行环境和机械反馈闭环。
- 预期防范的问题：99 分规则评审仍产出错乱页面；自由生成 HTML/CSS；多个脚本没有统一入口；技术绿灯冒充目标用户验证；交付缺少可复现源哈希和运行证据。
- 新增约束：统一执行 `lint → render → test → report`；生成只接受组件白名单、登记模板或 project-local 候选；发布同时要求 Contract、浏览器几何、任务、素材、视觉和追溯通过。
- 回归结果：v4.0 行为检查 23/23；AI 口语 1280×800 Golden Case 真实 Chromium 任务与视觉基线通过；exact/extensible/no_match 三态运行时检查通过；v3.4 25/25、v3.7 50/50、跨项目隔离 7/7 保持通过。
- 已知边界：未执行新的真实目标用户测试；技术通过仍只标记技术证据。外部模型连续会话复跑未在本次返回，未将其记为新通过证据。

### v3.11.0：结构化失败回流与有限修复

- 修改位置：Harness 错误模型、`schemas/harness-error.yaml`、`repair` 命令和失败注入回归。
- 修改原因：原型失败后缺少统一错误和局部重试边界，容易要求模型整体重写页面。
- 预期防范的问题：无限自愈；修复覆盖源 Contract；布局问题误改业务语义；失败没有最小复现。
- 新增约束：错误归一为 Contract、布局、流程、状态、主操作、素材、追溯、浏览器和视觉九类；只修复声明 `shrink-to-safe-area` 的局部几何，最多两次并保留源文件。

### v3.10.0：真实浏览器与 Golden Case 验证

- 修改位置：Chromium DOM 审计、任务自动执行、截图差异、AI 口语 1280×800 Golden Case 与 `tests/run_v40_harness_checks.py`。
- 修改原因：页面能生成或打开不代表无越界、无遮挡、主流程完整或恢复可用。
- 预期防范的问题：横向溢出、区域重叠、P0 不可见、多主操作、流程不可达、异步无兜底、视觉回退。
- 新增约束：真实浏览器检查全部页面和主任务；Golden Case 覆盖课程地图、目标、单词、重点句、对话、纠音、报告与具体恢复键。

### v3.9.0：确定性原型运行时与模板路由

- 修改位置：中性 HTML Shell、CSS/JS Runtime、模板 Registry、组件白名单和受控 Renderer。
- 修改原因：原 Generation Request 虽写明 controlled，下游仍可自由编写 HTML/CSS，布局规则没有执行者。
- 预期防范的问题：黑盒页面生成；CSS 人物或历史图片替代素材 Slot；新项目默认套旧模具；任意组件和动作进入原型。
- 新增约束：exact/extensible 只能选登记模板；no_match 使用通用网格和 project-local 候选；Golden Case 不自动成为共享模具。

### v3.8.0：项目身份、持久化 Snapshot 与可执行 Contract

- 修改位置：新增项目身份闸门、Snapshot/Identity/Frame/Flow schema、Snapshot 事件写入和对应主路由。
- 修改原因：新旧项目判断依赖上下文，PUI 只有语义区域，长对话后状态和页面几何无法机械校验。
- 预期防范的问题：跨项目污染；revision 覆盖；区域越界；状态不可达；异步无超时；“继续学习”没有具体恢复位置。
- 新增约束：先判定 resume/fork/new/needs_confirmation；S3 后持久化 Snapshot；Frame/Flow lint 未通过时禁止生成。

### v3.7.0：项目级模具与对话原型发布固化

- 修改位置：新增 50 项结构校验、用例 K、v3.7 发布基线；同步 README、quickstart、Dify、Coze 和 GPTs 配置。
- 修改原因：v3.5–v3.6 已形成 Profile、适配、候选、占位、对话生成与登记规则，但缺少统一发布门槛和平台配置映射。
- 预期防范的问题：主入口漏路由；平台跳过模具适配；文档仍按旧原型流程使用；结构完整却未做真实前向测试；局部新规则导致 v3.4 生命周期回退。
- 发布门槛：quick_validate、v3.4 25 项、v3.7 50 项、五类专项用例与正式运行时 50 轮均须通过，四维总分不低于 90；任何未执行证据不得写成已通过。
- 回归结果：Skill 结构校验通过；v3.4 生命周期 25/25、v3.7 模具体系 50/50、跨项目污染扫描 7/7、同一 PRS 三轮连续修改 8/8；最终完整正式运行时 50/50 通过硬检查。专项逐条语义记录 48/50，四维评分 99/100；全部基础约束、21/21 关键 TFD/Patch Contract 与源码完整性通过，发布候选门槛通过。证据绑定提交 `03086db` 与源码 SHA-256 `ea2b8bf8318d4bce0af7777a6ef738aa6035d066108aa0a45b2403f0348c652a`。
- 已知边界：Figma 连接仍需重新授权，本版本验证的是证据不可访问时的 fail-closed 行为，未把节点标题当作实际设计参数；真实原型渲染、用户测试、目录登记、Runtime Registry、工程消费和部署仍需各自证据。

### v3.6.1：候选模具生命周期与登记治理

- 修改位置：新增 `references/prototype-template-lifecycle.md`、`schemas/template-manifest.yaml` 和生命周期回归；更新 S5、模板目录、验证、跨 Skill 与 A2UI Bundle 协议。
- 修改原因：项目级候选能够生成后，如果没有证据门槛和版本状态，容易因“页面能打开”被直接升级为公共模具，并混淆目录登记、Runtime Registry、工程消费和生产部署。
- 预期防范的问题：无目标任务证据自动登记；未授权外部写入；验证后覆盖旧版本；停用模板继续进入候选；registered 被误报为生产可用。
- 新增约束：生命周期使用 project-candidate → in-validation → validated → registration-requested → registered → deprecated/superseded；登记必须有 TMF、Owner、适用范围、隔离/技术/任务证据和真实返回记录。
- 回归要求：生命周期用例、对话式原型、三态适配、跨项目隔离和既有 v3.4 生命周期检查全部通过。

### v3.6.0：对话式原型生成与自然语言回改

- 修改位置：新增 `references/conversational-prototype-session.md`、`schemas/prototype-generation-request.yaml` 和对话原型回归；更新 S4、中保真与跨 Skill 交接。
- 修改原因：即使已有 PUI、Profile 和模具适配，下游仍可能一次性自由生成 HTML，用户看不到模具与约束如何进入页面，修改也无法回写权威状态。
- 预期防范的问题：黑盒原型；重复询问已确认资料；自然语言修改只改截图不改 Contract；完成口径等 L3 变化在页面中静默生效；没有生成能力却声称预览完成。
- 新增约束：同一 `PRS-` 会话先展示生成前摘要，再发出受控 Generation Request；L1/L2 Patch 可回退，L3 变化必须创建 DEC/CHG；generated、技术验证、目标用户验证和生产状态分开。
- 回归要求：对话式原型三轮场景、三态模具匹配、跨项目隔离和既有 v3.4 生命周期检查全部通过。

### v3.5.2：素材占位与无匹配候选模具

- 修改位置：新增 `references/prototype-asset-slots.md`、`references/prototype-candidate-generation.md`，对应 ASC/PTC schema 和无匹配回归；更新 S4、PUI、中保真与模具适配协议。
- 修改原因：`no_match` 如果只返回阻塞，会诱导下游重新选择最相似旧模具；黑白原型若没有素材 Slot，也会用旧图片或临时装饰填满空白。
- 预期防范的问题：无匹配时强套旧模具；候选模具进入共享目录；P0 素材失败被伪装成成功；旧项目图片成为默认 fallback；占位没有比例、缺失和错误规则。
- 新增约束：`no_match` 使用通用网格和 `prototype-neutral` 生成 `project-local` 的 `PTC-`；影响布局的素材必须建立 `ASC-`，只标注语义、位置、比例、状态和替换规则。
- 回归要求：无匹配、完全匹配、可扩展匹配、跨项目隔离和既有 v3.4 生命周期检查全部通过。

### v3.5.1：模板目录与原型模具适配闸门

- 修改位置：新增模板目录、适配闸门、模板定义与适配决策 schema，以及完全匹配/可扩展匹配回归；更新 `SKILL.md`、PUI、中保真和 A2UI Runtime 协议。
- 修改原因：已有 Catalog Match 位于运行时阶段，无法阻止原型阶段按最近项目或视觉相似度直接套用旧模具。
- 预期防范的问题：只用总分或风格相似判定；设备、P0 和关键状态未比对；可扩展匹配改写共享模板；Runtime 静默覆盖原型阶段的无匹配结论。
- 新增约束：所有原型在生成前逐项比较目标用户、核心任务、页面类型、信息层级、关键区域、设备尺寸和交互状态；结果只使用 `exact / extensible / no_match`，硬阻塞项未知或不通过时不得放行。
- 回归要求：完全匹配、可扩展匹配、跨项目隔离和既有 v3.4 生命周期检查全部通过。

### v3.5.0：项目 UI Profile 与证据隔离

- 修改位置：新增 `references/project-ui-profile.md`、`schemas/project-ui-profile.yaml` 和跨项目隔离用例；更新 `SKILL.md` 与 `references/project-snapshot.md`。
- 修改原因：现有 PUI 能描述页面语义，DS Profile 能描述候选组件体系，但新项目缺少独立的 UI 上下文和外部设计证据映射，容易把历史项目文案、视觉资产或业务规则静默带入新原型。
- 预期防范的问题：新项目共享旧 `profile_id`；无法访问 Figma 时根据链接标题编造参数；“参考旧页面”被解释为继承全部内容；设计证据脱离当前项目 PUI、页面、功能和 DEC。
- 新增约束：每个项目使用独立 `UIP-`；外部证据记录来源与访问状态；文案、样例数据、用户数据、视觉资产、品牌 Token 和业务规则默认禁止继承。
- 回归要求：跨项目隔离用例必须通过；既有生命周期、低摩擦对话、教育 Profile 和五子棋交付逻辑不得退化。

### v3.4.8：双端设计系统与 AI UI Contract

- 修改位置：更新 `SKILL.md`、`references/design-system-architecture.md`、教育回归用例 F 和生命周期检查；同步 README。
- 修改原因：原设计系统协议以学习端 shadcn/Radix 为主要示例，后台与学生端没有明确分流，组件也缺少可供 AI 稳定解析的布局、插槽、变体和响应式字段。
- 预期防范的问题：教师后台误套用学习端 Profile；AI 只根据截图或组件名称猜测布局；不同页面自行发明 slots、variants 和状态；候选库被误报为已安装或已接入。
- 新增约束：教师/运营后台优先 Ant Design + ProComponents，学生/内容端优先 shadcn/ui + Radix + Tailwind Variants，严格多部件插槽时可选 Park UI + Ark UI + Panda CSS；DS Contract 必须包含 `layout_pattern`、`regions`、`slots`、`variants`、`states`、`responsive_rules`、`token_refs` 和 `traces_to`。
- 回归要求：用例 F 验证双端路由、AI Contract 字段、基础/产品/领域层边界和实现授权状态；生命周期结构检查应通过。

### v3.4.7：低摩擦对话与 Ready Gate 强化

- 修改位置：更新 `SKILL.md`、`references/low-friction-dialogue.md`、需求收敛/生命周期/账本/状态测试/生产交接规则；新增五角色增强用例集、正式回归记录与版本说明。
- 修改原因：v3.4.5 已隐藏大部分内部术语，但仍存在标题模板冲突、一个问句包含多个决定、跨项目上下文污染、短答历史重述，以及高影响 Pending 规则混入 Ready Must/AC 的风险。
- 预期防范的问题：把探索对话做成表单或小型 PRD；对教师、设计、运营和研发使用同一套术语；提前索要产物时生成占位规格；金额、权限或状态触发未定却写出可执行验收；范围优先级 Must 被误当成研发就绪。
- 回归结果：五角色各 10 轮、共 50 个独立正式运行时会话，50/50 通过增强硬检查；探索轮最多 1 个问题、最长 179 个汉字；独立四维评审 92/100，三个定向发布问题复审通过。
- 已知边界：最终补丁前的双 Gate 随机重复压力为 5/6；失败样本已修补，最终官方样本通过，但补丁后尚未重跑完整 6 次概率稳定性矩阵。

### v3.4.6：设计系统与学习组件架构协议

- 修改位置：新增 `references/design-system-architecture.md`；更新 `SKILL.md` 的 S4 设计系统路由、`DS-` 追溯、交付形态与质量门槛；扩展教育回归用例 F。
- 修改原因：中保真原型只能使用 `prototype-neutral`，缺少把基础组件、产品 Token 和学习领域组件分层交接的协议，容易把视觉样式、组件库选择与评分/解锁等业务规则混在一起。
- 预期防范的问题：把 shadcn/Radix 候选说成已安装；将学习组件名称误写为评分、录音或通关规则；把 SpeakUp Profile 套用到所有教育或后台产品；视觉 Token 反向覆盖已确认页面语义。
- 修改边界：基础组件库与 SpeakUp Profile 均为按需路由的候选规范；当前 Skill 仅产出 `DS-` Contract、Token 语义和组件映射，不安装包、不注册组件、不写生产工程代码。基础库、品牌 Profile 与实际接入均保持独立确认和授权。
- 回归要求：用例 F 必须保持教育 MVP、角色边界、状态/验收与未确认业务规则；同时验证基础层/产品层/领域层分离、组件追溯和候选实现边界。Skill 结构校验必须通过。

### v3.4.5：角色化低摩擦对话呈现层

- 修改位置：新增 `references/low-friction-dialogue.md`；更新 `SKILL.md` 的默认交互、S0–S3 路由与最小回复模板；补充 5W2H 路由说明、用例 I 和生命周期检查。
- 修改原因：50 轮无污染角色化基线显示，教师、产品、设计、运营和研发角色的回复频繁将 `S1/S2`、`Needs Decision`、`DEC`、`PUI` 等内部语言直接暴露给用户，且多个探索轮默认抛出两个独立问题，低摩擦原则没有形成执行层。
- 预期防范的问题：非专业用户被内部术语阻塞；角色化开场缺失；重复或复合提问；5W2H 退化为问卷；需求状态与用户可见表达混淆。
- 修改边界：内部 ProjectSnapshot、DEC/CHG、状态机、PUI Contract 和生产交接规则保持不变；仅增加面向用户的呈现层与路由，用户明确要求规格/交接时仍可展示稳定 ID。
- 回归要求：复跑 `tests/role-dialogue-50` 的五角色 50 轮基线；用例 A、E、I 与 v3.4 生命周期检查必须通过。
- 回归结果：同一无污染五角色矩阵已完成 50/50 正式运行时回归；探索表层的内部控制词从 15/20 降至 0/20，超过一个问题从 12/20 降至 0/20。详见 `tests/role-dialogue-50/v3.4.5-low-friction/review.md`。

### v3.4.4：原型 UI Contract 层

- 修改位置：新增 `references/prototype-ui-contract.md`；更新 `SKILL.md` 的 S3→S4 路由、`ProjectSnapshot`、中保真交接规范和对抗用例 H。
- 修改原因：页面规格进入中保真原型前缺少统一的页面骨架、信息层级、操作层级、组件语义、页面状态和上下游语义边界，原型容易直接滑向视觉稿或静默补业务规则。
- 预期防范的问题：主次操作不清、P0/P1/P2 缺失、状态出口遗漏、原型中视觉默认被误认为品牌方案、最终设计改动语义、Contract 与功能/验收不可追溯。
- 交付边界：新增 `PUI-` Contract 和 `prototype-neutral` 原型展示规范；不涉及生产组件库、Registry、MCP、前端实现或部署。
- 回归要求：用例 H 验证 Contract 在中保真前建立；v3.4 生命周期回归、五子棋用例 D 和 Skill 结构校验不得退化。

### v3.4.3：轻量总控与全周期回归

- 修改位置：重构 `SKILL.md` 为轻量总控；同步 README、Agent 元数据和 Dify/Coze/GPTs 映射；新增 `tests/v3.4-lifecycle-cases.md` 与 `tests/run_v34_lifecycle_checks.py`。
- 修改原因：主入口同时承载通用访谈、教育规则、A2UI、原型、运行时和工程交付细节，导致每次调用加载大量不相关上下文，主链路反而不清楚。
- 预期防范的问题：专项规则抢占核心流程、平台映射仍沿用旧四阶段、压缩入口时遗漏 DEC/状态/交付约束、只检查关键词却没有跨阶段回归场景。
- 回归要求：主入口不超过 220 行；所有新参考可从入口发现；H–L 五类场景、旧用例 D 及 Skill 结构校验均通过。
- 首次回归发现：用例 D 的已确认范围“好友互通”被近义改写，语义基本保留但稳定范围名称丢失，导致自动追溯失败；已将“权威命名原样保留”升级为 ProjectSnapshot 接口约束并安排复跑。
- 回归结果：v3.4 生命周期契约 18/18 通过；用例 D 重新执行真实 Codex 运行后四维 100/100，静态、运行时和下游产物检查全部通过。

### v3.4.2：产品运营闭环与专项路由

- 修改位置：新增 `references/product-operations-loop.md`；在 `SKILL.md` 增加 S7 路由，并接入 `$product-management-workflows` 的研究、竞品、路线图、利益相关方更新和指标复盘。
- 修改原因：现有 Skill 在 PRD/原型交付后缺少稳定的反馈入口，研究、指标和路线图容易形成孤立报告或第二份权威需求。
- 预期防范的问题：少量反馈直接变成需求、竞品功能直接抄入范围、路线图只加不减、指标相关性被写成因果、专项工作流重复生成 PRD。
- 回归要求：五个专项工作流只写回 ProjectSnapshot；Feature Spec 不建立平行 PRD；复盘必须形成 Continue/Adjust/Investigate/Stop 动作，基线变化必须创建 DEC/CHG。

### v3.4.1：中保真交互线稿与证据回写

- 修改位置：新增 `references/medium-fidelity-handoff.md`；更新跨 Skill、原型证据与生产交接规范。
- 修改原因：v0.3 能验证页面关系，但从结构线框到生产交接之间缺少稳定的中保真定义，真实信息密度、主次操作、异常恢复和 Mock 边界容易遗漏。
- 预期防范的问题：用高保真装饰掩盖规则缺口、页面样例静默改业务、技术回归被当作目标用户有效、原型发现未回写 DEC/CHG、产物没有 ART 追溯。
- 回归要求：Ready for Prototype 后才能进入 v0.4；主路径和关键异常可操作；技术、目标用户、交接证据分开；真实用户证据缺失时不得标记 v1.0。

### v3.4.0：统一生命周期与 ProjectSnapshot

- 修改位置：新增 `references/lifecycle-orchestration.md`、`references/project-snapshot.md` 和 `references/requirements-convergence.md`；重构 Skill 主链路。
- 修改原因：原四阶段需求流程与原型版本、跨 Skill Delivery、生产交接和运营活动分别维护状态，长周期项目容易出现多份相互冲突的“当前版本”。
- 预期防范的问题：需求 Ready 与原型 Ready 混用、文件生成被说成交付完成、变更只改页面不改 AC/测试、研发与运营使用不同基线。
- 回归要求：所有项目使用 S0–S7 与统一状态词表；单一 ProjectSnapshot 覆盖产品、证据、治理、架构与交付；每个 Must 保持目标到 ART 的稳定追溯。

### v3.3.0：结构原型交接与验证

- 修改位置：`SKILL.md` 原型递进交付、跨 Skill 交付协议；新增 `references/structural-prototype-handoff.md`。
- 修改原因：需求 Ready 后的 v0.3 常被直接跳成高保真或“文件已生成”，导致页面路径、锁定、恢复和 Mock 边界还没被验证就被固化。
- 预期防范的问题：把低保真样例当真实内容、把本地模拟说成服务已接入、锁定/完成/异常状态没有明确去向、内部走查被误称为真实学员测试。
- 回归要求：v0.3 交接必须包含页面范围、关键 DEC、适用状态、验证任务和回传审计；本地预览只证明原型可打开，不替代真实用户或生产证据。

### v3.2.0：A2UI Asset Bundle Handoff

- 修改位置：`SKILL.md` Asset Bundle Handoff 与运行规则；新增 `references/a2ui-asset-bundle-handoff.md`；扩展跨 Skill 交付路由。
- 修改原因：最终交付仍容易停留在散落 Markdown 或“文件已生成”的模糊结论，无法支持组件、Token、Schema、测试、注册与运行时消费的分层交接。
- 预期防范的问题：把本地候选说成 PR/上线、Bundle 缺失 P0 产物却标完整、注册/导出/消费/部署状态混淆、未授权外部写入。
- 回归要求：每轮正式运行 20 个真实 Skill 案例；Bundle 必须有稳定基线、Artifact 清单、生命周期证据、P0 追溯与外部动作授权状态；不得执行或声称未授权发布。
- 回归结果：v2.8.0–v3.2.0 各 20/20、合计 100/100 条独立正式运行记录通过基础运行约束；详见 `tests/iteration-regression/`。该结果不替代 Runtime、组件注册、远程分发或生产部署证据。
- 回归修复：完整交付用例 D 首次发现只读审计缺少显式外部动作声明；已在跨 Skill 返回审计增加固定收口语，并要求重跑用例 D。
- 完整回归结果：重跑用例 D 后静态、运行时和真实下游产物检查全部通过，四维评分 100/100；下游产物仍被正确判为 Blocked，不混同为生产发布完成。

### v3.1.0：A2UI Runtime Quality Gate

- 修改位置：`SKILL.md` Runtime Quality Gate 与运行规则；新增 `references/a2ui-runtime-quality-gate.md`。
- 修改原因：Studio/Contract 产物若没有受控 Schema、Catalog、修复上限与 fallback，容易把一次预览渲染误作稳定运行时能力，并允许模型临时产生未注册组件。
- 预期防范的问题：无限自愈、任意 Variant 渲染、敏感错误泄露、Catalog 缓存被误当部署证据、失败被静默展示为成功。
- 回归要求：每轮正式运行 20 个真实 Skill 案例；输出必须保持需求级边界，Runtime 候选默认 fail closed，有限修复、Catalog 匹配、fallback 与证据状态必须可审计。

### v3.0.0：A2UI Studio Patch Bridge

- 修改位置：`SKILL.md` Studio Patch Bridge 与运行规则；新增 `references/a2ui-studio-patch-bridge.md`；扩展跨 Skill Handoff Manifest。
- 修改原因：用户需要所见即所得调整和压力打靶，但直接把 GUI 操作写回业务规格会绕过 DEC/CHG，预览成功也容易被误称为运行时/发布完成。
- 预期防范的问题：任意 CSS 或未注册组件进入协议、并发 Patch 静默覆盖、L3 规则被面板直接改写、正常截图替代压力验证、Studio 与运行时状态混淆。
- 回归要求：每轮正式运行 20 个真实 Skill 案例；Patch 必须有基线、允许操作、L1/L2 回退与 ST-ID 验证；L3 操作必须转 Needs Decision。

### v2.9.0：防御性 UI Contract

- 修改位置：`SKILL.md` 防御性 UI Contract 与运行规则；新增 `references/a2ui-defensive-ui-contract.md`。
- 修改原因：P0/P1/P2 仅作为分析标签时，下游仍可能用示例数据堆出完整页面，长文本、空字段、加载、失败和权限边界缺少稳定验收。
- 预期防范的问题：P2 留白、P0 被超长内容挤出、空/失败状态伪装为成功、具体 CSS 技巧被误写成业务需求、压力测试无法回指功能和组件。
- 回归要求：每轮正式运行 20 个真实 Skill 案例；Contract 必须含 Slot 优先级、至少六类状态、三个适用压力用例与功能追溯；不生成实现代码。

### v2.8.0：A2UI 输入与决策协议

- 修改位置：`SKILL.md` 原型递进交付、运行规则和资产索引；新增 `references/a2ui-intake-decision-contract.md`；增强 50 轮正式测试运行器以支持受控的 20 轮批次。
- 修改原因：原 Skill 能分阶段收敛需求，但对一句话/图稿直投缺少统一输入事实、默认等级和字段优先级协议，容易退化为反复追问或把低风险视觉默认与高影响业务规则混在一起。
- 预期防范的问题：截图事实被误写为业务事实、默认决策不可回退、P0/P1/P2 缺失策略不一致、下游 A2UI 能力没有稳定输入边界。
- 回归要求：每轮正式运行 20 个真实 Skill 案例；每例不超过 2 个问题、包含不确定性标签、不得输出实现代码；A2UI 直投场景应给出 Draft、默认理由、字段优先级和一个 L3 缺口。

### v2.7.0：原型验证证据与测试会话规范

- 修改位置：`SKILL.md` 原型递进交付、运行规则与资产索引；新增 `references/prototype-validation-evidence.md`；新增对抗用例 G。
- 修改原因：v2.3.0 已要求技术验证与用户测试同时存在，但缺少可执行的证据分层、多人测试会话起点和结果回写规则，容易把回归通过误作用户有效，或让参与者之间的残留进度污染测试。
- 预期防范的问题：技术绿灯被表述为学习效果、未执行测试被写成通过、多人测试起点不一致、研究记录无法驱动下一轮页面/组件决策。
- 回归要求：用例 G 必须拒绝在缺少目标用户记录时标记 v1.0，并给出匿名任务记录、受控重置和 DEC/CHG 回写；教育内容变更同时回归用例 F，既有迭代闸门与每轮问题上限不退化。

### v2.6.0：跨 Skill 交付编排

- 修改位置：SKILL.md 角色边界、组件交付、生产交接和运行规则；新增 `references/cross-skill-delivery.md`；同步 README、Agent 元数据和 Dify/Coze/GPTs 映射。
- 修改原因：需求 Skill 能生成设计输入，但用户继续要求高保真原型、研发任务或测试资产时，缺少稳定的下游选择、输入冻结和返回审计协议。
- 预期防范的问题：下游能力静默补充业务规则、把文件生成误当成交付完成、专业实现被冒充为需求 Skill 自身能力、未获授权的外部发布。
- 回归要求：无专业产物请求时不启动编排；高影响歧义阻塞 Ready；能力不可用时只输出 Handoff Manifest；返回产物必须回指功能和已确认决策。

### v2.6.1：决策账本与变更影响追踪

- 修改位置：SKILL.md 确认协议、轮次状态、生产交接、质量门槛与输出模板；新增 `references/decision-ledger.md`；同步 README、Agent 元数据和 Dify/Coze/GPTs 映射。
- 修改原因：长对话中的“确认/是的”和后续回改缺少稳定指向，已有变更规则也没有定义历史保留和逐层传播状态。
- 预期防范的问题：短回复误确认多个规则、已确认结论被静默覆盖、业务改动只更新 PRD 却漏改页面/状态机/测试/下游产物。
- 回归要求：短回复只绑定唯一 Pending 决策；Confirm 进入 DEC 账本；修改创建新的 DEC 与 CHG，原决策保留为 Superseded；高影响传播完成前不得维持 Completed。

### v2.6.2：状态机和测试自动触发

- 修改位置：SKILL.md 业务流程、设计输入、生产交接、质量门槛和输出模板；新增 `references/state-test-gates.md`；补强生产交接清单、README、Agent 元数据和 Dify/Coze/GPTs 映射。
- 修改原因：此前状态机与测试大多依赖用户明确提出，实时多人、超时和并发规则可能直接进入原型，导致页面完整但业务状态不可验收。
- 预期防范的问题：倒计时被混用、并发事件出现多个成功结果、请求状态污染业务状态、状态图没有对应测试、Demo 替代业务规则。
- 回归要求：命中触发条件后自动建模；进入原型、研发和发布分别执行门槛；每个 Must 转移至少覆盖正向、守卫拒绝和恢复/终态测试。

### v2.6.3：五子棋完整回归用例 D

- 修改位置：`tests/adversarial-cases.md`、新增 `tests/gomoku-case-d/` 与 `tests/run_gomoku_case_d.py`；更新 SKILL.md 回归路由与 README。
- 修改原因：A/B/C 和既有 50 轮测试主要验证单轮问答与规则存在，不能证明“需求确认 → 决策账本 → 状态机/测试 → 跨 Skill 产物 → 返回审计”完整链路。
- 预期防范的问题：单项规则通过但组合交付失效、原型与需求脱节、并发/超时状态无测试、未验收工作被文件完整度掩盖。
- 回归要求：正式运行时读取当前 Skill 与三份真实产物；四维总分至少 90、任一维度至少 15；静态规则、运行回复和产物检查全部通过；历史教育内容场景保留为用例 F。
- 回归结果：用例 D 自动检查 100/100，正式回复自评 95/100，并正确将未达到 Ready 的下游产物判为 Blocked；A/B/C/E/F 代表场景 5/5 重跑通过，合并正式记录维持 50/50。

### v2.5.2：回退为自然语言确认

- 修改位置：SKILL.md 确认协议、初始化模板、对话规则与输出模板；quickstart、Dify/Coze/GPTs 配置和用例 E。
- 修改原因：点选、单选和多选会把开放式需求访谈误变为表单填写，并在实际对话中造成“全部选中”与互斥判断的额外负担。
- 预期防范的问题：用户被要求点击选择、记忆编号或在预设项中表达复杂需求；默认交互与自由讨论目标不一致。
- 回归要求：首轮以自然语言问题和示例引导，不出现默认选择控件、复选框或字母选项；用户可自由描述多个方向并自然语言回改。

### v2.5.1：紧凑点选与自然语言回改

- 修改位置：SKILL.md 选择式确认协议、初始化模板、对话规则与输出模板；quickstart、Dify/Coze/GPTs 配置和用例 E。
- 修改原因：v2.5.0 的稳定编号、说明文本和双问题并排展示增加了非专业用户的阅读与输入负担。
- 预期防范的问题：用户必须记忆 `Q1-A` 等编号、选项区过长、在同一轮被迫处理两个不相关决策、修改操作难以发现或再次输出冗长的完整清单。
- 回归要求：首轮默认一个决策组、2–4 个短标签；纯文本可用 `A C` 回答；普通回改只输出“已改 / 影响”（可加一条继续入口），且“去掉/改成”能保留未变选择。

### v2.5.0：多选优先的可回改确认协议

- 修改位置：SKILL.md 工作原则、初始化模板、对话运行流程、运行规则与输出模板；quickstart、Dify/Coze/GPTs 配置；新增用例 E。
- 修改原因：原 Skill 虽提供选择示例，但没有定义可多选的交互优先级、稳定选项标识、文本回退和后续修改机制，产品经理、设计师和运营难以快速确认并调整多项需求。
- 预期防范的问题：把并存需求误做成单选、因平台无控件无法完成选择、一次修改导致已确认决策被重置、选项变更无法追溯到流程/功能/页面/验收影响。
- 回归要求：用例 E 必须验证“多选 → 回改 → 仅更新受影响范围”；A/B/C/D 保持每轮至多两个问题、事实/假设边界和阶段门槛。

### v2.4.21：单一权威待确认问题列表

- 修改位置：SKILL.md 对话运行流程与轮次状态维护规则。
- 修改原因：正式平台真实运行中发现，推荐方案回复会在“待确认项”和“下一步确认”两个区块重复同一问题。
- 预期防范的问题：避免重复追问、问题数量虚增和用户不知道哪个问题需要回答。
- 回归要求：重跑正式平台 50 轮记录，并确认每轮只保留一个权威待确认问题列表。

### v2.4.1：当前项目教育功能基线

- 修改位置：新增 references/project-functional-playbook.md；新增 tests/adversarial-cases.md 用例 D；SKILL.md 教育场景路由。
- 修改原因：当前项目已有高考备考专区界面资产，但功能分析缺少项目事实、候选对象和待确认边界。
- 预期防范的问题：把视觉卡片直接等同于业务功能，凭空补齐学生、运营、题库和推荐能力。
- 回归要求：普通项目不加载该基线；教育内容项目先区分事实、候选和待确认。

### v2.4.2：教育内容对象分类

- 修改位置：SKILL.md 教育场景路由。
- 修改原因：当前项目卡片同时表达真题、模拟、课程和资料，原功能拆解容易把内容类型混为一个 Card。
- 预期防范的问题：内容对象生命周期、使用动作和验收标准互相污染。
- 回归要求：只在教育内容场景启用，不改变普通 SaaS 的对象建模。

### v2.4.3：内容生命周期

- 修改位置：SKILL.md 教育内容对象规则。
- 修改原因：现有生命周期示例没有覆盖教育内容的审核、过期和下线入口。
- 预期防范的问题：学生看到不可用内容，运营无法解释发布/撤回后的可见性。
- 回归要求：具体状态名称可按用户业务调整，不能默认所有内容都需要审核。

### v2.4.4：学科与阶段关系

- 修改位置：SKILL.md Stage 2 教育内容流程规则。
- 修改原因：当前页面同时有学科 Tabs 和备考阶段，原规则没有区分筛选维度与学习路径。
- 预期防范的问题：学科切换误改学习进度，阶段卡片误当作内容分类。
- 回归要求：非教育内容项目不引入学科/阶段概念。

### v2.4.5：内容卡片主任务

- 修改位置：SKILL.md Stage 4 页面与卡片规则。
- 修改原因：当前项目大量信息通过卡片承载，但卡片视觉标签没有说明真实点击任务。
- 预期防范的问题：用户点击后不知道进入详情、预览、作答、下载还是外链。
- 回归要求：未确认去向时标记待确认，不凭空绑定路由或技术实现。

### v2.4.6：备考阶段进度

- 修改位置：SKILL.md 阶段推进规则。
- 修改原因：当前页面有四阶段导航，但未区分阶段分组、学习进度和解锁逻辑。
- 预期防范的问题：用户误以为点击阶段即可完成任务，阶段完成条件无法验收。
- 回归要求：阶段是内容分组时不得强行增加进度系统。

### v2.4.7：学习记录状态

- 修改位置：SKILL.md 教育内容对象分类。
- 修改原因：展示页迭代成学习产品时，点击、开始、完成和收藏容易被混成一个行为。
- 预期防范的问题：学习进度虚高、继续学习入口错误、收藏与完成状态互相覆盖。
- 回归要求：用户未确认学习记录范围时保持候选状态。

### v2.4.8：作答与反馈对象

- 修改位置：SKILL.md 工作原则与教育功能规则。
- 修改原因：真题/模拟内容要形成学习闭环，必须区分作答、提交、评分和反馈。
- 预期防范的问题：把打开试卷当成完成、凭空承诺自动判分、无法支持重交和复习。
- 回归要求：未确认的评分方式、时限和批改角色保留为待确认。

### v2.4.9：推荐来源透明

- 修改位置：SKILL.md 教育内容推荐规则。
- 修改原因：备考专区容易把“精选”与“个性化推荐”混为一谈。
- 预期防范的问题：推荐依据不明、用户无法关闭、没有推荐时页面断裂。
- 回归要求：无推荐能力的项目不自动增加推荐模块。

### v2.4.10：学习者与运营端分层

- 修改位置：SKILL.md 角色化开场与教育场景规则。
- 修改原因：当前页面是学习者入口，但真正可运营的内容产品还需要内容生产和审核链路。
- 预期防范的问题：只做前台展示无法维护内容，或未经确认强行增加后台系统。
- 回归要求：运营端始终标为候选角色，需用户确认后进入功能矩阵。

### v2.4.11：内容可见与可用权限

- 修改位置：SKILL.md 教育内容生命周期规则。
- 修改原因：内容卡片可见并不代表用户有权打开、下载或作答。
- 预期防范的问题：地区/年级/年份/订阅边界泄露，用户点击后才发现不可用。
- 回归要求：具体资格规则仍需用户确认，不默认引入订阅系统。

### v2.4.12：预览下载权限

- 修改位置：SKILL.md 教育内容卡片规则。
- 修改原因：真题/资料卡片可能同时支持预览、下载、分享，但三者权限和版权边界不同。
- 预期防范的问题：内容泄露、下载链接失效无提示、版权和离线行为未定义。
- 回归要求：没有下载/分享能力时不增加额外功能。

### v2.4.13：继续学习恢复

- 修改位置：SKILL.md 学习记录规则。
- 修改原因：有进度状态但没有说明用户如何从首页继续，尤其是多设备或内容失效场景。
- 预期防范的问题：进度显示与实际入口不一致、用户被带回错误位置。
- 回归要求：用户未确认学习记录时保持候选，不默认实现同步。

### v2.4.14：错题复习关联

- 修改位置：SKILL.md 作答与反馈规则。
- 修改原因：结果反馈如果不能回到原题和复习任务，就无法形成备考闭环。
- 预期防范的问题：错题重复、来源丢失、复习状态无法验收。
- 回归要求：未确认错题功能时不自动加入功能矩阵。

### v2.4.15：内容关系类型

- 修改位置：SKILL.md 学科/阶段关系规则。
- 修改原因：课程、真题和资料之间的先修、替代、系列关系不能都称为相关推荐。
- 预期防范的问题：学生学习路径错误、下线内容没有替代入口、关系循环无法解释。
- 回归要求：只有用户确认存在内容关系时才展开。

### v2.4.16：教育内容发现筛选

- 修改位置：SKILL.md 列表/教育内容规则。
- 修改原因：当前页面有学科和阶段入口，但没有明确哪些是筛选、分组或路径。
- 预期防范的问题：筛选条件重复、组合逻辑不明、用户无法清空或解释无结果。
- 回归要求：只对存在内容发现任务的教育项目启用。

### v2.4.17：内容标签时效

- 修改位置：SKILL.md 教育内容可见性规则。
- 修改原因：当前页面使用“2026最新”等标签，但没有来源、判定时间和失效规则。
- 预期防范的问题：过期内容仍显示最新、运营无法维护标签、营销词被误当排序逻辑。
- 回归要求：标签语义需用户确认，不能从视觉文案推导事实。

### v2.4.18：学习指标口径

- 修改位置：SKILL.md 成功指标规则。
- 修改原因：教育产品常展示完成率/正确率，但缺少分子、分母、版本和隐私口径。
- 预期防范的问题：不同页面数字不一致、用户被错误比较、历史版本混算。
- 回归要求：只对需要学习数据或看板的项目启用。

### v2.4.19：内容运营交接

- 修改位置：SKILL.md 设计输入与生产交接规则。
- 修改原因：学习者端内容功能没有反向连接到内容创建、审核、发布和替换流程。
- 预期防范的问题：前台内容无法维护，版本替换影响学习记录却没有回滚方案。
- 回归要求：运营角色需用户确认后再输出完整后台功能。

### v2.4.20：项目用例 D 回归闸门

- 修改位置：SKILL.md 持续优化与对抗评估规则。
- 修改原因：当前项目已建立高考备考专区功能基线，但通用 A/B/C 回归没有覆盖项目事实与教育学习闭环。
- 预期防范的问题：通用规则全部通过，却把当前页面错误扩展成未经确认的学习/运营系统。
- 回归要求：当前项目后续功能迭代必须同时回归用例 D。
### v2.3.1：GitHub 对标证据路由

- 修改位置：SKILL.md「标杆与开源对标」；新增 references/github-benchmarks.md。
- 修改原因：开源项目对标缺少统一的官方来源、可迁移模式和事实/推断边界。
- 预期防范的问题：把仓库实现当成需求结论、一次性搬运过多能力、引用失去来源。
- 回归要求：对标仍按需启用，未请求对标时不增加无关提问或输出。

### v2.3.2：对象先于菜单

- 修改位置：SKILL.md Stage 3 功能架构分层。
- 修改原因：参考 Plane 的工作项与多视图模式，修复先列菜单、后补业务对象造成的功能重复。
- 预期防范的问题：同一对象在列表/看板/详情中被拆成多个伪功能，生命周期和数据关系无法追踪。
- 回归要求：保留用户指定的导航和页面输出，但先补对象与状态。

### v2.3.3：工作项责任链

- 修改位置：SKILL.md 功能矩阵规则。
- 修改原因：参考 OpenProject 的工作包与项目角色关系，原矩阵容易把“使用角色”误当成唯一责任人。
- 预期防范的问题：需求无人负责、审批与执行混淆、关联对象无法追溯。
- 回归要求：普通功能仍可使用单一使用角色，不强行增加无关责任字段。

### v2.3.4：层级状态与权限继承

- 修改位置：SKILL.md 信息架构规则。
- 修改原因：参考 Outline 的文档/集合层级与权限继承模式，原 IA 规则没有要求标明继承断点。
- 预期防范的问题：草稿、发布、归档和子节点权限被隐式混合，导致内容不可见或越权。
- 回归要求：普通导航仍保持简洁，只有存在层级内容或组织边界时启用。

### v2.3.5：组件变体与来源

- 修改位置：SKILL.md 组件清单规则。
- 修改原因：参考 Penpot 的组件、Variants 与共享 Library 模式，原组件表只有变体名称，缺少变体轴和维护来源。
- 预期防范的问题：组件变体无限膨胀、设计与开发使用不同来源、复用组件无人维护。
- 回归要求：保留 AntD/Tailwind 需求级映射，不要求生成设计源文件。

### v2.3.6：对话决策增量

- 修改位置：SKILL.md 对话运行流程。
- 修改原因：长对话中重复复述全部结论会掩盖本轮真正变化。
- 预期防范的问题：用户无法判断哪些内容已改变，确认成本持续上升。
- 回归要求：仍保留完整的三份状态，不牺牲阶段结论。

### v2.3.7：提问理由显式化

- 修改位置：SKILL.md 工作原则。
- 修改原因：原规则要求问题有理由，但没有强制在用户可见输出中说明。
- 预期防范的问题：用户把关键问题误解成无关盘问，降低回答质量。
- 回归要求：问题总量仍不超过 2 个。

### v2.3.8：多种回答入口

- 修改位置：SKILL.md 非专业用户表达规则。
- 修改原因：只给自然语言问题仍可能让教师或业务方不知道回答颗粒度。
- 预期防范的问题：用户因不会使用术语而沉默，或被迫猜测标准答案。
- 回归要求：保留事实、假设和待确认边界，不把选项视为用户确认。

### v2.3.9：阶段推进门槛

- 修改位置：SKILL.md 四阶段工作流结尾规则。
- 修改原因：已有阶段确认语句，但缺少进入下一阶段的最小输入门槛。
- 预期防范的问题：阶段跳跃、功能矩阵建立在未确认目标之上。
- 回归要求：低风险缺口可标记待确认，不要求一次清空所有问题。

### v2.3.10：歧义影响分级

- 修改位置：SKILL.md 评审与交付规则。
- 修改原因：高影响闸门已存在，但中低影响缺口缺少继续推进的判定方式。
- 预期防范的问题：低风险问题阻塞全部进度，或中风险假设被误写成事实。
- 回归要求：高影响项仍必须阻塞 Ready。

### v2.3.11：核心对象清单

- 修改位置：SKILL.md Stage 3 产品功能架构。
- 修改原因：对象优先原则仍缺少统一的对象字段。
- 预期防范的问题：对象名称、所有权、生命周期和可见范围散落在页面描述中。
- 回归要求：对象不明确时标记待确认，不凭空补齐。

### v2.3.12：业务不变量

- 修改位置：SKILL.md Stage 3 功能规则。
- 修改原因：功能列表有状态，但缺少跨功能始终成立的业务约束。
- 预期防范的问题：不同页面分别实现规则，导致金额、状态或权限互相矛盾。
- 回归要求：不把技术校验代码写入需求文档。

### v2.3.13：状态与事件分离

- 修改位置：SKILL.md Stage 2 业务流程步骤字段。
- 修改原因：仅记录状态容易遗漏谁在何时触发了状态变化。
- 预期防范的问题：审计、通知、重试和异常恢复无法追溯。
- 回归要求：简单流程可用表格表达，不强行生成事件架构。

### v2.3.14：时间与时区规则

- 修改位置：SKILL.md Stage 2 流程步骤字段。
- 修改原因：预约、周期和结算业务容易只写日期，不写生效、截止和时区语义。
- 预期防范的问题：跨时区截止错误、逾期状态不一致、周期计算无法验收。
- 回归要求：无时间约束的普通项目不增加额外输出。

### v2.3.15：数据权威与冲突

- 修改位置：SKILL.md 核心对象清单。
- 修改原因：多系统或多入口产品容易出现同一对象多份数据，但原规则没有指定权威来源。
- 预期防范的问题：覆盖写入、同步循环、人工修正无记录。
- 回归要求：只在存在多个数据来源或同步关系时启用。

### v2.3.16：页面主任务

- 修改位置：SKILL.md Stage 4 页面规格。
- 修改原因：页面目标和核心操作并列时，无法判断视觉焦点与主按钮。
- 预期防范的问题：页面操作竞争、主流程不清、设计稿缺少任务优先级。
- 回归要求：保留复杂页面的多个次要操作，但明确唯一主任务。

### v2.3.17：组件状态转移

- 修改位置：SKILL.md 组件规格表字段。
- 修改原因：只有状态名称无法指导何时进入、如何退出和如何恢复。
- 预期防范的问题：加载、成功、失败、禁用状态之间的交互断裂。
- 回归要求：保留组件变体与事件字段。

### v2.3.18：表单恢复路径

- 修改位置：SKILL.md 组件规格中的表单规则。
- 修改原因：表单字段定义通常覆盖输入，却遗漏未保存、重复提交和失败恢复。
- 预期防范的问题：用户丢失输入、重复创建、失败后无法继续。
- 回归要求：非表单页面不增加表单专属字段。

### v2.3.19：列表批量操作状态

- 修改位置：SKILL.md 多视图与页面状态规则。
- 修改原因：列表场景容易只写筛选和分页，遗漏批量操作的确认、进度和部分失败。
- 预期防范的问题：误操作、长任务无反馈、失败项无法重试或导出。
- 回归要求：无批量操作的列表不强行添加批处理流程。

### v2.3.20：响应式取舍

- 修改位置：SKILL.md 页面规格。
- 修改原因：响应式要求如果只有一句“适配移动端”，无法支持设计和验收。
- 预期防范的问题：信息被简单压缩、关键操作消失、表格在小屏不可用。
- 回归要求：按目标平台选择需要说明的屏宽和降级行为。

### v2.3.21：可访问性可验收

- 修改位置：SKILL.md 页面规格中的响应式/无障碍规则。
- 修改原因：原规则提到无障碍，但缺少可观察的焦点、播报和非颜色反馈标准。
- 预期防范的问题：键盘用户无法完成任务、状态只依赖颜色、设计无法验收。
- 回归要求：按页面交互复杂度选择具体无障碍条目。

### v2.3.22：内容密度规则

- 修改位置：SKILL.md 页面与功能追溯规则。
- 修改原因：页面结构完整但文本长度、空值和格式未定义，无法评估真实信息密度。
- 预期防范的问题：长标题溢出、金额/时间误读、空值被误认为异常。
- 回归要求：只对有结构化内容和多语言/格式差异的页面展开。

### v2.3.23：搜索权限与新鲜度

- 修改位置：SKILL.md Stage 3 共享能力规则。
- 修改原因：搜索常被当成一个输入框，遗漏范围、权限过滤和结果新鲜度。
- 预期防范的问题：越权泄露、搜索结果过期、无结果和失败无法解释。
- 回归要求：非搜索型项目不自动增加搜索模块。

### v2.3.24：通知触发与去重

- 修改位置：SKILL.md Stage 3 共享能力规则。
- 修改原因：通知需求容易只写发送渠道，遗漏触发、接收人、重复和失败策略。
- 预期防范的问题：重复轰炸、关键状态只存在通知里、通知失败后无人知晓。
- 回归要求：通知仍按业务价值启用，不默认加入所有项目。

### v2.3.25：空状态分类

- 修改位置：SKILL.md Stage 4 页面状态规则。
- 修改原因：空、无权、失败和暂不可用被混成一种 Empty 状态，用户无法采取正确下一步。
- 预期防范的问题：错误引导、无权限被误报为空、失败状态没有重试或申诉入口。
- 回归要求：简单页面可合并展示，但必须保留语义差异。

### v2.3.26：认证与授权分层

- 修改位置：SKILL.md Stage 2 权限规则。
- 修改原因：登录身份、操作权限、数据范围和临时授权容易被合并成一个角色字段。
- 预期防范的问题：已登录用户被误认为可操作，撤权后权限继续生效。
- 回归要求：普通项目仍可用简化权限矩阵，但需明确简化假设。

### v2.3.27：敏感数据生命周期

- 修改位置：SKILL.md 质量门槛。
- 修改原因：教育、员工和支付场景已有权限规则，但缺少数据目的、保存和删除边界。
- 预期防范的问题：过度收集、长期保留、导出/删除责任不明。
- 回归要求：仅在涉及个人或敏感数据时启用。

### v2.3.28：审计事件字段

- 修改位置：SKILL.md Stage 2 业务流程步骤字段。
- 修改原因：事件已有发起者和结果，但缺少审计所需的目标、原因和关联上下文。
- 预期防范的问题：无法解释谁为何改变了关键对象，异常调查没有证据链。
- 回归要求：普通浏览行为不强制生成审计事件。

### v2.3.29：并发冲突策略

- 修改位置：SKILL.md Stage 3 业务不变量。
- 修改原因：对象规则没有覆盖多人编辑或异步更新造成的旧数据覆盖。
- 预期防范的问题：静默覆盖、用户不知道保存的是旧版本、冲突无法恢复。
- 回归要求：单人、同步且无共享编辑的项目不强行引入冲突流程。

### v2.3.30：外部调用可靠性

- 修改位置：SKILL.md 页面/交互交接规则。
- 修改原因：外部服务和长任务只描述成功/失败，遗漏幂等、超时、取消和重复回调。
- 预期防范的问题：重复创建、任务无限等待、失败后无可操作反馈。
- 回归要求：仅在存在外部服务或异步长任务时启用。

### v2.3.31：验收标准结构化

- 修改位置：SKILL.md Stage 3 功能规格。
- 修改原因：验收标准虽要求可测试，但缺少统一的前置条件、动作和结果结构。
- 预期防范的问题：验收口径主观、异常和权限边界遗漏。
- 回归要求：允许用户指定其他验收格式，不强行改写既有 AC。

### v2.3.32：指标可测量性

- 修改位置：SKILL.md 5W2H/成功指标规则。
- 修改原因：价值结果已有方向选项，但没有基线、目标、窗口和数据来源。
- 预期防范的问题：上线后无法判断是否达成目标，指标变成形容词。
- 回归要求：探索阶段允许标记待验证。

### v2.3.33：里程碑进入退出条件

- 修改位置：SKILL.md Master PRD 版本计划结构。
- 修改原因：路线图只写阶段名称，无法判断何时进入或完成。
- 预期防范的问题：阶段目标漂移、依赖未完成却提前承诺上线。
- 回归要求：保持路线图为需求级计划，不生成排期承诺。

### v2.3.34：风险责任与触发

- 修改位置：SKILL.md Master PRD 风险结构。
- 修改原因：风险列表如果没有触发信号和责任人，无法在项目中执行。
- 预期防范的问题：风险无人监控、缓解措施不可追踪、残余风险被隐藏。
- 回归要求：低风险探索项可简化字段，但不能伪造概率。

### v2.3.35：假设决策责任

- 修改位置：SKILL.md 质量门槛。
- 修改原因：待确认项已能阻塞交付，但缺少谁在何时做决定以及未决保护动作。
- 预期防范的问题：关键假设长期悬置，团队误以为可以无条件开发。
- 回归要求：只对高影响待确认项强制记录责任和期限。

### v2.3.36：集成边界

- 修改位置：SKILL.md 技术架构候选规则。
- 修改原因：跨系统责任与人工兜底不够明确，接口字段又容易被凭空补齐。
- 预期防范的问题：数据所有权不明、失败责任互相推诿、未确认字段被伪造。
- 回归要求：只在用户需要技术方案或存在外部集成时启用。

### v2.3.37：同步异步边界

- 修改位置：SKILL.md 技术架构候选规则。
- 修改原因：接口/事件并列描述，容易遗漏异步通知、顺序和最终一致性。
- 预期防范的问题：重复回调、超时重试、状态短暂不一致无法验收。
- 回归要求：技术实现仍标记为候选，不指定消息中间件。

### v2.3.38：批量数据结果

- 修改位置：SKILL.md 列表/批量操作规则。
- 修改原因：导入导出常只描述文件上传，遗漏部分成功、错误报告和回滚。
- 预期防范的问题：脏数据进入系统、失败项无法定位、批量操作不可撤销。
- 回归要求：无批量数据能力的项目不增加导入导出流程。

### v2.3.39：第三方降级路径

- 修改位置：SKILL.md 集成边界规则。
- 修改原因：第三方依赖只写成功路径，缺少不可用、限流、凭证过期和版本变化处理。
- 预期防范的问题：外部服务故障直接变成用户无法解释的系统错误。
- 回归要求：仅对关键外部依赖启用，普通内部功能不强行添加。

### v2.3.40：非功能要求量化

- 修改位置：SKILL.md 技术架构候选规则。
- 修改原因：非功能要求容易停留在“高性能/高可用”等形容词。
- 预期防范的问题：容量、时效、安全和恢复能力无法评审或验收。
- 回归要求：根据项目风险选择相关非功能维度，不强制填写全部项。

### v2.3.41：对标来源可追溯

- 修改位置：SKILL.md 标杆与开源对标输出。
- 修改原因：已有事实/推断区分，但矩阵行没有强制来源和核验日期。
- 预期防范的问题：开源能力被过期摘要或二手文章误述。
- 回归要求：未启用对标时不增加来源字段。

### v2.3.42：变更影响映射

- 修改位置：SKILL.md 质量门槛。
- 修改原因：变更记录已有版本信息，但没有要求说明对下游交付物的影响。
- 预期防范的问题：改了业务规则却漏改页面、组件或验收标准。
- 回归要求：影响范围不确定时保留待评估，不虚构追溯关系。

### v2.3.43：版本回滚影响

- 修改位置：SKILL.md 终态 Master PRD 追溯结构。
- 修改原因：版本计划和变更影响已有规则，但缺少回滚及其数据/权限影响。
- 预期防范的问题：版本回退后对象状态、权限或历史数据不一致。
- 回归要求：仅在存在多个版本或可回滚发布时启用。

### v2.3.44：设计输入可追溯

- 修改位置：SKILL.md 设计输入包规则。
- 修改原因：页面骨架和组件规格存在，但没有强制回指功能 ID 和未决视觉决策。
- 预期防范的问题：设计稿与需求脱节、视觉稿暗含未确认业务规则。
- 回归要求：用户未要求设计输入时不增加交接字段。

### v2.3.45：交接状态与责任

- 修改位置：SKILL.md 评审与交付规则。
- 修改原因：生产交接字段齐全，但缺少整体可执行状态和阻塞责任。
- 预期防范的问题：未准备好的功能被误交给开发，阻塞项没有处理人。
- 回归要求：Ready 仍受高影响歧义和验收标准门槛约束。

### v2.3.46：结论证据标签

- 修改位置：SKILL.md 标准输出模板。
- 修改原因：事实/假设分离规则没有统一到每条关键结论。
- 预期防范的问题：候选建议被误读为用户确认，结构化文档产生虚假确定性。
- 回归要求：标签使用中文人话，不增加专业用户的无效负担。

### v2.3.47：问题预算可追踪

- 修改位置：SKILL.md Prompt Rules。
- 修改原因：提问上限存在，但长对话中没有显式防止重复占用问题预算。
- 预期防范的问题：重复追问已确认内容、单轮问题数量隐性超限。
- 回归要求：不要求用户看到内部计数器，只保证输出遵守上限。

### v2.3.48：实现边界再确认

- 修改位置：SKILL.md Prompt Rules。
- 修改原因：范围边界在角色定义中存在，但用户直接要求代码时缺少就地处理方式。
- 预期防范的问题：需求分析 Skill 越界生成生产代码或把接口示意当成实现。
- 回归要求：保留需求级接口/数据模型说明能力。

### v2.3.49：平台配置约束一致性

- 修改位置：SKILL.md 版本与评估资产索引。
- 修改原因：平台配置映射可能只保留角色描述，丢失提问、证据、Ready 和范围约束。
- 预期防范的问题：Dify/Coze/GPTs 之间行为不一致，导出后 Skill 退化。
- 回归要求：平台差异只做字段格式映射，不复制一套分叉 Prompt。

### v2.3.50：发布候选总闸门

- 修改位置：SKILL.md 质量门槛。
- 修改原因：已有四维量表和 Ready 检查，但缺少把 A/B/C、来源、平台和高影响决策统一起来的发布闸门。
- 预期防范的问题：单个用例高分却存在跨场景回退，或平台导出后约束丢失。
- 回归要求：未满足发布条件时明确标记迭代版，不伪称生产终版。
### v2.2.1：核心任务回答支架

- 修改位置：`SKILL.md`「建立项目画像」首轮核心任务问题。
- 修改原因：仅要求用户回答角色、场景和任务，仍可能让非专业用户不知道从哪里开始。
- 预期防范的问题：用户继续用“做个高级系统”泛化回答，首轮无法得到可追踪的真实任务。
- 回归要求：保持每题一个核心决策、每轮最多 2 个问题，不提前生成完整 PRD。

### v2.2.2：价值结果选择器

- 修改位置：`SKILL.md`「建立项目画像」首轮结果口径问题。
- 修改原因：非专业用户常能描述想做什么，却难以抽象“成功指标”。
- 预期防范的问题：价值目标被空泛地写成“高级、好用”，无法支撑优先级判断。
- 回归要求：允许用户自定义结果或暂记待确认，不把示例选项当成既定业务规则。

### v2.2.3：部分回答承接

- 修改位置：`SKILL.md`「建立项目画像」事实/假设/待确认规则。
- 修改原因：用户常只回答一个问题或给出半句描述，原规则没有明确如何继续承接。
- 预期防范的问题：重复提问、重启访谈、增加用户认知负担并丢失已确认信息。
- 回归要求：保持分阶段确认和每轮最多 2 个问题。

### v2.2.4：设计师入口单决策化

- 修改位置：`SKILL.md` 角色化开场中的设计师入口。
- 修改原因：设计师入口原本同时追问任务、信息层级和状态，仍可能跳过需求确认直接进入 UI 讨论。
- 预期防范的问题：首轮信息过载、视觉方案先于业务任务、未确认状态被误当成设计事实。
- 回归要求：保留设计输入阶段的 IA、状态和组件映射能力。

### v2.2.5：动效规格可交接化

- 修改位置：SKILL.md Stage 4 动效规则。
- 修改原因：原规则要求说明动效触发和降级，但未要求目标属性、时长/缓动和减少动态效果的替代表现。
- 预期防范的问题：设计师与前端对动效强度理解不一致，动效在无障碍场景下不可用。
- 回归要求：不生成实现代码，保留需求级组件映射和 UI 状态覆盖。

### v2.2.6：多视图状态一致性

- 修改位置：SKILL.md 组件体系与页面状态规则。
- 修改原因：数据大屏/控制台的多视图切换容易遗漏筛选、加载、错误、空态和刷新时间的状态边界。
- 预期防范的问题：切换视图后状态串线、数据新鲜度不明、不同视图错误地共享未确认的筛选条件。
- 回归要求：保持 Tabs、Drawer、Skeleton、Empty、Error 等组件语义映射。

### v2.2.7：多租户数据范围权限

- 修改位置：SKILL.md Stage 2 角色与业务流规则。
- 修改原因：仅列操作权限无法表达 SaaS 租户、机构、渠道之间的数据隔离。
- 预期防范的问题：角色看似有权限矩阵，但跨租户/机构查询、切换和越权反馈没有边界。
- 回归要求：保留普通项目的角色—目标—任务映射，不强行引入多租户概念。

### v2.2.8：资金逆向流程闭环

- 修改位置：SKILL.md Stage 2 业务流程规则。
- 修改原因：分润和退款场景如果只描述主流程，无法覆盖撤回、冲正、重复请求和部分成功。
- 预期防范的问题：资金状态不可追溯、退款逆向链路断裂、并发或人工复核边界缺失。
- 回归要求：仅对金额/积分/额度/分润/退款类对象启用，不把技术实现伪装成既定方案。

### v2.2.9：高影响歧义交付闸门

- 修改位置：SKILL.md Stage 8 评审与交付规则。
- 修改原因：已有待确认项记录，但缺少对“不可标记为已确认/Ready”的硬闸门。
- 预期防范的问题：文档格式完整却掩盖范围、角色、主流程或验收口径尚未确定。
- 回归要求：低影响假设仍可标注后继续，高影响歧义必须输出阻塞决策表。

### v2.2：首轮问题颗粒度补丁

- 修改位置：`SKILL.md`「建立项目画像」首次回应规则。
- 修改原因：用例 A 中原规则虽限制为 2 个问题，但每个问题包含多个独立决策，业务小白仍容易被信息量阻塞。
- 预期防范的问题：形式上遵守提问上限、实际上一次追问过多维度；过早追问平台和技术约束，导致核心业务任务尚未确认。
- 回归要求：保持教师/业务专家的通俗引导、每轮最多 2 个问题、阶段确认，以及 B/C 用例的架构、组件和异常推演规则不退化。

### v2.1：非专业用户上手与生产交接

- 修改位置：`SKILL.md` 快速上手模式、角色化开场、Ready/Done 规则和交付索引；新增 `references/quickstart.md`、`references/production-handoff.md`、`references/versions/v2.1-onboarding-handoff.md`。
- 修改原因：让教师、业务专家和设计师可以用自然语言直接开始，并将分析结果转化为设计、开发、测试可执行的交接包。
- 预期防范的问题：非专业用户被术语阻塞、需求看似完整但无法开发/验收、关键假设在交接时被静默补全。
- 验证状态：待执行 Skill 结构校验和术语/交接字段覆盖审计。

### Role Prompt：四阶段专家角色合并

- 修改位置：`SKILL.md` Role、四阶段标准工作流、初始化启动响应、MoSCoW 功能矩阵、终态 Master PRD 模板；`agents/openai.yaml` 默认启动提示。
- 修改原因：将产品架构师角色定义和四阶段交付规范统一进当前生产入口，避免角色 Prompt 与 Skill 工作流分叉。
- 预期防范的问题：重复询问已知上下文、阶段跳跃、功能矩阵缺少用户故事/异常处理、UI 输出无法交接。
- 回归要求：保持每轮最多 2 个问题、A/B/C 对抗用例和 D1–D4 资产链接不退化。

### D4：回归与平台固化

- 修改位置：`platform/`、`tests/regression-report.md`、`references/versions/v2.0-release.md`。
- 修改原因：完成 v2.0 发布候选所需的跨平台配置映射和回归证据沉淀。
- 预期防范的问题：平台迁移时 Prompt 规则丢失、评分缺少证据、发布版本无法追溯。
- 回归结果：A/B/C 规则级评分均达到 90 分以上；结构校验通过。正式生产发布前需在目标平台补录真实 3–4 轮对话记录。

### D3：组件体系映射补丁

- 修改位置：`SKILL.md`「沉淀组件与设计系统需求」视觉与组件规则。
- 修改原因：让 Ant Design/Tailwind 等指定体系能映射到可交接的组件语义和 Token，而不是只输出抽象组件名。
- 预期防范的问题：设计规格与产研组件库脱节、误把需求级规格写成前端实现代码。
- 回归用例：用例 B/C，并检查用例 A 的通俗引导和提问上限未退化。

### D2：引导控制补丁

- 修改位置：`SKILL.md`「建立项目画像」首次提问规则。
- 修改原因：修复首次回应列出 3 个问题、违反每轮最多 2 个问题硬约束的问题。
- 预期防范的问题：业务小白场景中的信息过载和对话阻塞。
- 回归用例：用例 A，并检查 B/C 仍遵守分步输出、架构分层和范围边界。

- 修改位置：`SKILL.md` Prompt Rules、对话流程和版本资产索引。
- 修改原因：将持续优化方案固化为可重复执行的“场景注入 → 模拟跑通 → 评审挑刺 → 定向补丁”闭环。
- 预期防范的问题：一次性重写导致逻辑回退、问题数量过载、缺陷无法追溯、版本无法复盘。
- 新增资产：四维评分量表、A/B/C 对抗用例、Meta-Prompt、v1.0/v1.1/v1.2/v2.0 版本快照。
- 回归状态：结构校验通过；引用资产、硬约束和占位符审计通过。A/B/C 动态对话评分按 Meta-Prompt 执行，作为后续专项补丁的输入。
### v2.3.0：从需求文档到可验证原型包的递进交付

- 修改位置：`SKILL.md` 四阶段工作流后、终态输出模板。
- 修改原因：一次性生成“最终 PRD + 最终页面”会掩盖结构、状态、视觉、组件和用户验证之间尚未确认的差异。
- 预期防范的问题：原型只有展示价值、缺少状态验证和交接追溯；把技术自动化通过误写成学习效果已验证。
- 新增规则：按 v0.1–v1.0 分阶段交付；每轮固定输出目标、范围、决策、影响、验证、问题和下一步；v1.0 必须同时拥有技术证据与目标用户任务测试结果。
- 回归要求：保持每轮最多 2 个问题、四阶段门槛、Ready/Done 闸门和跨 Skill 返回审计不退化。
